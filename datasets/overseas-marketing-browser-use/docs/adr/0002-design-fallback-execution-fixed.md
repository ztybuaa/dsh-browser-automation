# ADR-0002：fallback 只用于设计阶段

## 状态

Accepted

## 背景

Jackery 某些型号或主题可能在公开网页上信息不足。运行时自动换品牌、产品或主题会改变答案空间，破坏 benchmark 的可比性。

## 决策

设计阶段可以探索、放宽、替换并淘汰候选主题，以找到信息密度足够且营销价值明确的固定案例。案例冻结后，执行阶段禁止 fallback；素材不足必须如实呈现。

## 结果

fallback 是题目筛选机制，不是 Browser Use 的运行能力。候选探索过程写入研究记录，不进入执行 prompt。
